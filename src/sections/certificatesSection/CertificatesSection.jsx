import styles from "./CertificatesSection.module.scss";

const CertificatesSection = ({ lang, dictionary }) => {
  return <section className={styles.section}>CertificatesSection</section>;
};

export default CertificatesSection;
